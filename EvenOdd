# input() accepts user text; int() converts it into an integer number
number = int(input("Enter a whole number: "))

# Checking if the number is perfectly divisible by 2
if number % 2 == 0:
    print(f"{number} is an Even number.")
else:
    print(f"{number} is an Odd number.")
